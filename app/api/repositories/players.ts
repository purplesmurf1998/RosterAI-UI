import { PlayerCreation } from "@/lib/types/players";

export const insertPlayers = async (creations: PlayerCreation[]) => {

}

export const addPlayerAsTeamAdmin = async (playerId: string, teamId: string) => {

}

export const removePlayerAsTeamAdmin = async (playerId: string, teamId: string) => {
  
}