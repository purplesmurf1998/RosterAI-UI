import { Season } from "@/lib/types/seasons";

export const insertSeason = async (teamId: string, name: string) => {
  
}

export const addPlayersToSeason = async (teamId: string, seasonId: string, playerIds: string[]) => {

}